#include "main.h"

/**
 * swap_int - Write a functaion that swaps the values of two integers.
 * @a: The first int input
 * @b: The second int input
 *
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	int swap = *a;
	*a = *b;
	*b = temp;
}
